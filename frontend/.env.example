# Local development — backend runs on port 3001
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Production (Vercel) — set this in the Vercel dashboard environment variables
# NEXT_PUBLIC_API_BASE_URL=https://celo-mind-nmk2.onrender.com

# Reown AppKit project ID (get one at https://cloud.reown.com)
NEXT_PUBLIC_PROJECT_ID=
